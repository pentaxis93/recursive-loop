---
description: Resume parked work with mandatory context validation
---

Resume a parked project safely. Validates context integrity before any implementation.

Reads parking context from `03-resources/parking/`, runs 5-point validation check, and only proceeds when all checks pass.

Use when: returning to previously parked work.

Invoke skill: parking (unpark mode)
